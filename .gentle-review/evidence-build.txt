Verification evidence for nesting distance auto-accept UI under the master switch (branch downgrade/v0.3.28):

1. Build: .\gradlew.bat assembleDebug -> BUILD SUCCESSFUL (35 tasks) after MainActivity.kt edit. Only pre-existing deprecation warnings (TabRow/Divider); no new errors.
2. Diff scope: app/src/main/java/com/example/projectacc/MainActivity.kt (distance section wrapped in if (isPicapAutoAcceptEnabled)) + .gentle-review artifact updates. No changes to MyAccessibilityService.kt or OrderStateManager.kt in this diff.
3. Behavioral checks: runtime gating at MyAccessibilityService.kt:320 (isPicapAutoAcceptEnabled && shouldAutoAccept) predates this change and already made condition 3 inert when the master switch is off; this diff is presentation-only (visibility), slider/switch state persistence untouched via OrderStateManager.setAutoAcceptByKmEnabled/setAutoAcceptMaxKm.
4. Layout check: with master off, tab shows master switch then Spacer then order card; the divider previously above the distance section moved inside the conditional so no orphan divider remains.
5. No automated unit test suite exists for these modules; verification is compile + package success plus the reliability lens review (0 BLOCKER/CRITICAL, 1 SUGGESTION).
