Verification evidence for Picap auto-accept service-type filter (branch downgrade/v0.3.28, version 0.3.30):

1. Build: .\gradlew.bat assembleDebug -> BUILD SUCCESSFUL (35 tasks) after edits to OrderStateManager.kt, MyAccessibilityService.kt, MainActivity.kt. Only pre-existing deprecation warnings (TabRow/Divider); no new warnings or errors.
2. Diff scope: 3 tracked source files + .gentle-review review artifacts (tracked since 0f4dfef).
3. Behavioral checks:
   - Filter gate runs FIRST in shouldAutoAccept, before ganancia/km conditions; excluded orders log and return false without side effects.
   - shouldAutoAccept has exactly one call site (MyAccessibilityService.kt:320), gated by isPicapAutoAcceptEnabled -> filter is Picap-only.
   - WhatsApp auto-accept (whatsapp* settings, NotificationInterceptorService) has no reference to picapAutoAcceptFilter -> unchanged.
   - Filter default "TODOS" preserves previous behavior for existing installs and new users.
   - Filter persisted via SharedPreferences key picap_auto_accept_filter and reloaded in loadAll().
   - UI: SingleChoiceSegmentedButtonRow with OMS / Mostrador / Todos, rendered under the Auto-Aceptar switch only when enabled; SegmentedButton is stable in material3 (BOM 2026.02.01), no OptIn required.
4. No automated unit test suite exists for these modules; verification is compile + package success plus the reliability lens review (0 BLOCKER/CRITICAL, 3 SUGGESTIONs).
